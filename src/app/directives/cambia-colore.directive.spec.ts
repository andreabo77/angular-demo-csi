import { CambiaColoreDirective } from './cambia-colore.directive';

describe('CambiaColoreDirective', () => {
  it('should create an instance', () => {
    const directive = new CambiaColoreDirective();
    expect(directive).toBeTruthy();
  });
});
